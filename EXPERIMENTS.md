# Experiment index

Auto-generated by `EXPERIMENTS.md` regeneration; do not hand-edit.

`CONF` = protocol locked to git **before** results (the project's standing
rule). `EXPL` = exploratory, no pre-registered protocol — these are labelled
as such in their own `analysis.md` and their conclusions carry less weight.

| id | type | protocol commit | verdict |
|---|---|---|---|
| `h1-leak-fix-validation` | CONF | `d1f557f` | h1 analysis — first post-leak-fix run under the frozen evaluation |
| `h4-adaln-rtg-conditioning` | CONF | `350c0a3` | REFUTED, with a caveat, and a sharper diagnosis |
| `h5-score-head-bottleneck` | CONF | `cc2b753` | REFUTED on the real tests, and the refutation is the finding |
| `h6-frozen-dt` | CONF | `a9e9b4d` | PRIMARY AND FINAL ANALYSIS (pre-registered n=30) |
| `h7-decision-divergence` | CONF | `93429a0` | continued training changes ~18% of decisions |
| `h8-rtg-in-distribution` | CONF | `7aaaa78` | the RTG finding SURVIVES the OOD correction |
| `h9-rtg-band-width` | CONF | `538160d` | VOID by its own pre-registered manipulation check, and that is useful |
| `h10-rtg-raw-signal` | CONF | `e58764b` | VOID, and the void yields the actual structural answer |
| `h11-real-history-inference` | CONF | `6d8c285` | arms A/B interpretable (null), arm C VOID; and the void has a cause |
| `h12-kg-incumbent-reward` | CONF | `19edf14` | GATE FAILED (G2). Stopped as pre-registered. |
| `h13-kg-dense-reward` | CONF | `0b3c2bd` | gate PASSES; decomposition rule was badly chosen |
| `h14-joint-mes-reward` | **EXPL** | `—` | joint MES is already implemented, and it dominates on signal health |
| `h15-fair-reward-gate` | CONF | `3e68123` | the reproduction check FAILED, and it reversed |
| `h16-reward-headtohead` | CONF | `25887d0` | joint MES wins on the fair axis. Both locked predictions PASS. |
| `h17-joint-mes-frozen-eval` | CONF | `840fcc8` | the joint-MES reward gives the best MF-DRO result on record, and still FAILS the frozen test |
| `h18-deterministic-rollouts` | CONF | `735c91b` | GATE FAILED (G2). Stopped as pre-registered. The failure is the finding. |
| `h19-decoupled-eps-alpha` | CONF | `aa1f9dc` | gate PASSES, and the pre-registered NULL fires. The strongest negative yet. |
| `h20-mlp-score-head` | CONF | `10a5977` | removing the linear bottleneck does NOT restore conditioning |
| `h21-trained-vs-random-encoder` | CONF | `09bd7ea` | the encoder's contraction is architectural; the head's is LEARNED |
| `h22-state-gain-threshold` | CONF | `7e37083` | no threshold exists within two orders of magnitude |
| `h23-state-independent-component` | CONF | `8dcf5e6` | the asserted explanation is CONFIRMED, and now quantified |
| `h24-identify-the-rule` | CONF | `c4d6833` | the learned rule is uncertainty-AVERSE |
| `h25-sigma-sign-robustness` | CONF | `3ca1d1b` | the negative sigma_H sign is robust; its magnitude is not |
| `h26-btg-sensitivity` | CONF | `a2557e3` | BTG and extreme-RTG sensitivity: the last two unmeasured claims |
| `h27-sliding-window-inference` | CONF | `9da7950` | GATE FAILED (G2). Stopped as pre-registered; no frozen-eval run. |
| `h28-teacher-sigma-preference` | CONF | `eb175c8` | the uncertainty aversion is INHERITED. My "inversion" claim is retracted. |
| `h29-teacher-regime-sweep` | CONF | `d1fdc19` | the teacher's exploitativeness is INTRINSIC, not a tuning artefact |
| `h30-collinearity-suppression` | **EXPL** | `—` | `w[sigma_H] < 0` is a PARTIAL coefficient, not an aversion |
| `h31-teacher-without-dt` | CONF | `83ab3c2` | (running) |
| `h32-distillation-fidelity` | CONF | `e27fc66` | the distillation is FAITHFUL. My prediction failed. |
| `h33-fidelity-policy-gap` | CONF | `a22520e` | the fidelity channel is uninformative AND miscalibrated |

**31 experiments — 29 confirmatory, 2 exploratory.**

## Outcomes worth counting

- **4 voided** on their own pre-registered manipulation checks (h9, h10,
  h11 arm C, h18) — reported, never interpreted.
- **3 gates failed and stopped** before spending the expensive run (h12,
  h18, h27), saving two frozen-evaluation grids.
- **5 claims retracted or corrected after re-measurement**: h5's state swap
  (probe compared a state with itself), h18's eps/alpha coupling (diversity
  metric omitted query locations), h28's "student inverted its teacher"
  (it imitates), h30's aversion reading (partial coefficient under
  collinearity), h32's "lossy distillation" (argmax agreement is a bad
  metric under near-ties).
- **1 locked prediction of mine failed** (h32) and is reported as a failure.
